# S04-P05 — Incoming and Outgoing Transition Blueprint

> **Status:** LOCKED  
> **Phase:** S04-P05-R3 — Responsive Continuous Handoff Reconciled Transition Blueprint  
> **Authority:** Preserves all verified locks from `SCENE-04-PRODUCTION-BLUEPRINT.md`, `S04-P01-NARRATIVE-VISUAL-LOCK.md`, `S04-P02-CAMERA-SHOT-BIBLE.md`, `S04-P03-T04`, `S04-P04-T01`, and `cameraKeyframes.ts`.

---

## A. Transition Summary

Scene 04 connects the overall portfolio narrative via two explicit, single-owner transition boundaries:
1. **Incoming Boundary (Scene 03 → Scene 04)**: Accepts the completed digital product output seed from Scene 03 onto the Scene 04 laptop receiving dock (`[0.0, 0.4, 0.10]`), symbolizing the transition from **Product Assembly Engine** to **Project Proof Gallery**.
2. **Outgoing Boundary (Scene 04 → Scene 05)**: Extracts an authentic interface component fragment from the active project's preview surface (`[-0.20, 0.10, 0.05]` relative to active capsule position) into Scene 05's tablet device receiver (`[0.0, 0.0, 0.062]`), symbolizing the transition from **Macro Project Proof** to **Micro UX Thinking**.

---

## B. Incoming Ownership Contract (Scene 03 → Scene 04)

### 1. Responsibility Separation

- **Transferred Object:** Product Assembly Output Seed (generated by Scene 03 `ProductAssemblyBaseline`).
- **Receiver:** Laptop Receiving Dock (`LaptopDevice.tsx` / `deviceGroupRef` at `[0.0, 0.4, 0.10]`).
- **Arrival Feedback:** Arrival Receiver Node pulse glow (`arrivalGlowOpa` / `arrivalPulseT` during `localProgress 0.00 – 0.22`).

### 2. Transition Parameters

| Contract Attribute | Specification |
| :--- | :--- |
| **Global Progress Range** | `0.420000` to `0.484400` |
| **Scene-Local Progress Range** | Scene 03 (`0.85 – 1.00`) → Scene 04 (`0.00 – 0.12`) |
| **Camera Owner** | `CameraDirector` (`SCENE_03_EXIT` → `SCENE_04_BEAT_01`) |
| **Ownership Switch Point** | Single point at `globalProgress = 0.470000` (Scene 04 start boundary) |
| **Receiver Transform Owner** | `deviceGroupRef` position `[0.0, 0.0, 0.0]` with damped breathing float |
| **Dock Visibility Owner** | `dockOpacity = reducedMotion ? 0.4 : localProgress < 0.12 ? approachOpacity : Math.max(0, 1.0 - (p - 0.12) / 0.15) * approachOpacity` |
| **Dock Unmount Lifecycle** | Legacy panels unmount at `localProgress > 0.255` (`dockOpacity <= 0.1`); Laptop dock container unmounts at `localProgress > 0.27` (`dockOpacity <= 0.005`) |
| **Visible Systems** | Arrival receiver pulse node, Laptop chassis, Proof rail origin silhouette |
| **Hidden Systems** | HUD overlay text, active capsule proof layers, secondary/distant capsules |
| **Reverse-Scroll Behavior** | Deterministic restoration of dock opacity and arrival pulse glow at `p < 0.27` |

---

## C. Outgoing Ownership Contract (Scene 04 → Scene 05)

### 1. Shared Responsive Resolution Rule

$$\text{UX Transfer World Anchor} = \text{Active Capsule World Position} + \text{Extraction Local Offset } [-0.20, 0.10, 0.05]$$

### 2. Final Responsive Anchor Table

| Viewport Category | Active Capsule Position $[X, Y, Z]$ | Local Offset $[X, Y, Z]$ | UX Transfer World Anchor $[X, Y, Z]$ |
| :--- | :--- | :--- | :--- |
| **Desktop (`1440×900`, `1280×720`)** | `[1.55, 0.10, 0.00]` | `[-0.20, 0.10, 0.05]` | **`[1.35, 0.20, 0.05]`** |
| **Tablet (`1024×768`)** | `[1.55, 0.10, 0.00]` | `[-0.20, 0.10, 0.05]` | **`[1.35, 0.20, 0.05]`** |
| **Mobile (`430×932`, `390×844`, `375×667`)** | `[0.00, 0.00, 0.00]` | `[-0.20, 0.10, 0.05]` | **`[-0.20, 0.10, 0.05]`** |

### 3. Continuous Handoff & Ownership Switch

- **Owner Before `0.590000` (`globalProgress 0.572000 – 0.590000` / `S04 localProgress 0.85 – 1.00`)**: Active capsule UX extraction origin marker (`Scene04ProjectsGreybox.tsx`) positioned at the viewport-specific world anchor.
- **Owner At `0.590000` (`globalProgress = 0.590000`)**: Same object, same viewport-specific world position (Desktop/Tablet: `[1.35, 0.20, 0.05]`, Mobile: `[-0.20, 0.10, 0.05]`) with **zero spatial displacement**.
- **Owner After `0.590000` (`globalProgress 0.590000 – 0.626000` / `S05 localProgress 0.00 – 0.30`)**: Scene 05 lerping traveling UX object (`Scene05ProductUX.tsx` lerps from the viewport-specific source anchor to `tabletUXTarget` `[0.0, 0.0, 0.062]`).
- **Reverse-Scroll Restoration**: Ownership returns at the exact same viewport-specific anchor with zero spatial jump, smoothly contracting the marker back into the active capsule preview surface.

### 4. Transition Parameters

| Contract Attribute | Specification |
| :--- | :--- |
| **Global Progress Range** | `0.572000` to `0.626000` |
| **Scene-Local Progress Range** | Scene 04 (`0.85 – 1.00`) → Scene 05 (`0.00 – 0.30`) |
| **Camera Owner** | `CameraDirector` (`SCENE_04_BEAT_07` → `SCENE_05_APPROACH`) |
| **Visible Systems** | UX extraction origin marker, active capsule frame, proof rail tail curve |
| **Hidden Systems** | Secondary & distant capsules (faded at `p > 0.88`), HUD overlay text |

---

## D. Locked Camera Reference Table

All camera values match the locked production camera in [cameraKeyframes.ts](file:///d:/PORT/src/portfolio3d/camera/cameraKeyframes.ts):

| Stage | Global Progress | Viewport | Position $[X, Y, Z]$ | Target $[X, Y, Z]$ | FOV |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Scene 03 Exit (Beat 7 End)** | `0.470000` | Desktop | `[3.90, 0.15, 5.60]` | `[1.70, 0.00, 0.00]` | $48^\circ$ |
| | | Mobile | `[1.20, 0.05, 7.00]` | `[0.00, -0.20, 0.00]` | $44^\circ$ |
| **Scene 04 Beat 01 (Incoming Follow)** | `0.470000 – 0.4844` | Desktop | `[0.80, 0.30, 7.20]` | `[0.10, 0.30, 0.10]` | $50^\circ$ |
| | | Mobile | `[0.00, 0.20, 8.50]` | `[0.00, 0.30, 0.10]` | $46^\circ$ |
| **Scene 04 Beat 06 (Hero Readability Lock)** | `0.567200` | Desktop | `[0.95, 0.08, 2.35]` | `[0.95, 0.08, 0.00]` | $48^\circ$ |
| | | Mobile | `[0.00, 0.00, 3.80]` | `[0.00, 0.00, 0.00]` | $42^\circ$ |
| **Scene 04 Beat 07 (UX Extraction Endpoint)** | `0.590000` | Desktop | **`[0.30, 0.18, 7.80]`** | **`[0.00, 0.00, 0.00]`** | **$52^\circ$** |
| | | Mobile | `[0.00, 0.20, 8.20]` | `[0.00, 0.00, 0.00]` | $48^\circ$ |
| **Scene 05 Approach** | `0.590000 – 0.6260` | Desktop | `[0.20, 0.10, 8.00]` | `[0.00, 0.00, 0.00]` | $52^\circ$ |
| | | Mobile | `[0.20, 0.10, 8.00]` | `[0.00, 0.00, 0.00]` | $52^\circ$ |

---

## E. Forward Timeline

1. **`0.420000 – 0.470000` (Scene 03 Exit)**: Scene 03 completes product assembly output seed. Scene 04 is hidden (`opacity = 0.0`).
2. **`0.470000 – 0.484400` (Beat 01 Handoff)**: Scene 04 starts. Laptop dock receives seed at `[0.0, 0.4, 0.10]`; arrival pulse glow activates (`localProgress 0.00 – 0.22`).
3. **`0.484400 – 0.502400` (Beat 02 Gallery Reveal)**: Camera moves laterally. Proof rail tube geometry activates. Dock opacity fades (`0.12 → 0.27`).
4. **`0.502400 – 0.520400` (Beat 03 Establishment)**: Legacy screen panels unmount at `p > 0.255`. Laptop dock unmounts at `p > 0.27`. Active capsule establishes at `[1.55, 0.1, 0.0]` (desktop) or `[0.0, 0.0, 0.0]` (mobile).
5. **`0.520400 – 0.567200` (Beats 04–06 Inspection & Hero)**: Active capsule opens structural layers, presents proof, and locks Hero composition (`31.50%` HUD / `9.41%` gap / `53.50%` project lane).
6. **`0.567200 – 0.590000` (Beat 07 UX Extraction)**: UX extraction marker activates at responsive anchor `[1.35, 0.20, 0.05]` (desktop) / `[-0.20, 0.10, 0.05]` (mobile) (`p > 0.85`). Secondary capsules fade out (`p > 0.88`).
7. **`0.590000 – 0.626000` (Scene 05 Approach)**: Continuous ownership switch at `0.590000` at the responsive anchor. Scene 04 unmounts (`p >= 1.0`). Scene 05 lerping traveling packet takes over ownership along line from responsive anchor to `TabletDevice` (`[0.0, 0.0, 0.062]`).

---

## F. Reverse Timeline

1. **`0.626000 – 0.590000` (S05 → S04 Reverse)**: Scene 05 traveling packet contracts back toward the viewport-specific shared anchor (`[1.35, 0.20, 0.05]` desktop, `[-0.20, 0.10, 0.05]` mobile).
2. **`0.590000 – 0.567200` (Beat 07 Reverse)**: Single ownership switch at `0.590000` at the responsive anchor (zero spatial jump). Scene 05 packet unmounts (`S05 localProgress <= 0.0`); Scene 04 remounts (`localProgress < 1.0`), contracting UX extraction marker into capsule preview surface.
3. **`0.567200 – 0.502400` (Beats 06–03 Reverse)**: Active capsule transitions smoothly back through proof, inspection, and establishment states.
4. **`0.502400 – 0.484400` (Beat 02 Reverse)**: Laptop dock container remounts at `p < 0.27`. Legacy panels remount at `p < 0.255`.
5. **`0.484400 – 0.470000` (Beat 01 Reverse)**: Laptop dock and arrival pulse glow return to initial receipt state.
6. **`0.470000 – 0.420000` (S04 → S03 Reverse)**: Scene 04 unmounts (`opacity = 0.0`). Camera returns seamlessly to Scene 03 product assembly view.

---

## G. Mobile and Reduced Motion

- **Mobile Viewport (`< 768px`)**:
  - In normal motion, `dockOpacity` reaches `1.0` during early approach (`0.00 – 0.12`) before fading (`0.12 – 0.27`).
  - Active capsule position is centered at `position = [0.0, 0.0, 0.0]` (`activePos = isMobile ? [0.0, 0.0, 0.0] : [1.55, 0.1, 0.0]`).
  - Outgoing UX extraction marker aligns vertically at `[-0.20, 0.10, 0.05]`.
- **Reduced Motion (`reducedMotion = true`)**:
  - `dockOpacity` is held at `0.4` statically without 3D breathing float animations.
  - Static fallback card (`Scene04ProjectsStaticCard.tsx`) handles overlay presentation.
  - Cross-fades occur via React state and CSS opacity transitions on static card overlays.

---

## H. Corrected Reuse / Replace Matrix

| Component / Node | Source File | Boundary / Spatial Role | Decision |
| :--- | :--- | :--- | :--- |
| **Proof Rail (`railCurve`)** | `src/portfolio3d/components/Scene04ProjectsGreybox.tsx` | Trajectory 3D Catmull-Rom tube curve | **REUSE** |
| **Active Capsule Placeholder** | `src/portfolio3d/components/Scene04ProjectsGreybox.tsx` | Dominant hero project placeholder | **REPLACE (S04-P06)** |
| **Supporting Capsule Placeholders** | `src/portfolio3d/components/Scene04ProjectsGreybox.tsx` | Secondary & distant depth capsules | **REPLACE (S04-P06)** |
| **UX Extraction Origin Marker** | `src/portfolio3d/components/Scene04ProjectsGreybox.tsx` | Detaching UX surface origin (responsive anchor) | **RECONFIGURE (S04-P06/P07)** |
| **Scene 05 Source Anchor (`laptopProjectsSource`)** | `src/portfolio3d/constants/scene05Config.ts` | Source anchor for S05 lerp line | **RECONFIGURE (to responsive resolver function in S05 phases)** |
| **Debug Guides (`s04_p02_reference_anchors`)** | `src/portfolio3d/scenes/Scene04Projects.tsx` | Camera & safe-zone debug wireframes | **DEBUG-ONLY** |
| **Laptop Receiving Dock (`LaptopDevice.tsx`)** | `src/portfolio3d/components/LaptopDevice.tsx` | Handoff receiving dock chassis | **REUSE** |
| **Legacy Case Study Panels (`ProjectCaseStudyPanel.tsx`)** | `src/portfolio3d/components/ProjectCaseStudyPanel.tsx` | Temporary screen panels | **REMOVE-LATER (S04-P08)** |

---

## I. Implementation Dependencies

- **S04-P06 (Project Capsule Architecture)**: Must replace `active_capsule_greybox`, `secondary_capsule_greybox`, and `distant_capsule_greybox` while preserving the exact `[1.55, 0.1, 0.0]` anchor (mobile `[0,0,0]`) and `[-0.20, 0.10, 0.05]` UX extraction origin.
- **S04-P07 (Project Capsule Motion)**: Must implement the structural opening animation during `localProgress 0.42 – 0.60`.
- **S04-P08 (Project Content & HUD)**: Must attach HUD text overlay during `localProgress 0.27 – 1.00`.
- **S05 Implementation Phases**: Must replace static `SCENE_05_ANCHORS.laptopProjectsSource` in `src/portfolio3d/constants/scene05Config.ts` with a responsive resolver function `resolveScene05SourceAnchor(isMobile: boolean)` that evaluates `isMobile ? [-0.20, 0.10, 0.05] : [1.35, 0.20, 0.05]` so the lerp line connects seamlessly to the active project's UX extraction origin with zero spatial displacement across desktop, tablet, and mobile.

---

## J. Ambiguity Register

- **Unresolved Transition Ownership Ambiguities**: None.
- **Verdict**: No unresolved transition-ownership ambiguity remains to block `S04-P06 — Project Capsule Architecture`.

---

## K. Changed Files

- `docs/scene04/S04-P05-TRANSITION-BLUEPRINT.md` (UPDATED)
- `docs/scene04/evidence/tools/create_s04_p05_diagrams.ts` (UPDATED)
- `docs/scene04/evidence/S04-P05/incoming-transition-diagram.png` (REGENERATED)
- `docs/scene04/evidence/S04-P05/outgoing-transition-diagram.png` (REGENERATED)
- `docs/scene04/evidence/S04-P05/transition-ownership.json` (UPDATED)

---

S04-P05 LOCKED — The Scene 04 → Scene 05 UX handoff now uses source-verified responsive transfer anchors derived from the active capsule position and extraction offset, preserving zero-displacement forward and reverse ownership across desktop, tablet, and mobile without changing runtime behavior.
