# Handoff Notes — P6-T10

* **Downstream Alignment:** Pass immediately to `P6-T11: Experience timeline section structural coding`.
* **Guardrail Enforcement:** The upcoming career milestone display task in `P6-T11` must capture identical maximum reading widths (`max-w-[1200px]`, `px-6`) and the deep charcoal color scheme (`bg-[#0e0f11]`), structuring the vertical career timeline branch lines symmetrically to preserve the global surface rhythm.
* **ID Conflict Note:** `id="process"` is already used by `ArchitectureAssembly`. This section uses `id="workflow"`. Downstream sections must audit for duplicate ID conflicts before assigning anchor IDs.
