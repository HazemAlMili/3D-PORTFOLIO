# Handoff Notes — P6-T07

* **Downstream Alignment:** Pass output files immediately to `P6-T08: Deep case study preview bridge section structural coding`.
* **Guardrail Enforcement:** The upcoming section tracking milestone in `P6-T08` must directly extend the vertical margin parameters (`py-24`, `space-y-12`) and layout constants (`max-w-[1200px]`, `px-6`, `border-[#242830]`) locked here, framing the primary case study redirection controls symmetrically to prevent structural line breaks.
* **URL Binding Note:** The `liveUrl` and `codeUrl` fields in the `portfolioProjects` data array are currently set to `'#'`. Final URL values must be injected in the Phase 9 polish pass once production deployments are confirmed.
