# Handoff Notes — P5-T01

* **Downstream Alignment:** Pass output files immediately to `P5-T02: Scroll state engine + R3F canvas setup`.
* **Guardrail Enforcement:** The upcoming logic scaffolding milestone in `P5-T02` must directly leverage these mapped directories and package nodes, initializing the central `ScrollStateContext.tsx` and `SceneContainer.tsx` structures safely inside the designated files.
